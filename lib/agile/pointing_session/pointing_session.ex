defmodule PointingSession do
  def start(id) do
    # todo
  end

  def join(id, name) do
    # todo
  end

  def leave(id, name) do
    # todo
  end

  def vote(id, name, points) do
    # todo
  end

  def clear_votes(id) do
    # todo
  end
end
